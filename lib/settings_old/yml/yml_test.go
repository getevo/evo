package yml

import (
	"testing"
)

func TestYaml_Get(t *testing.T) {
	/*	var cfg = Yaml{}
		fmt.Println(cfg.Init("./test.yml"))

		var cfg = fiber.Config{}
		g := generic.Parse(&cfg)

		for _, field := range g.Props() {
			cfg.Register()
		}
		fmt.Println(cfg)*/
}
